\section{Results}
\label{sec:results}

We report the identical analysis for the two co-equal forms, Raw and Improved,
and our central finding is the contrast between them. Table~\ref{tab:overall}
summarizes the headline agreement numbers.

\begin{table}[!t]
\centering
\caption{Overall agreement, Raw vs.\ Improved (N=139 each).}
\label{tab:overall}
\begin{tabular}{lcc}
\toprule
Measure & Raw & Improved \\
\midrule
Composite $\kappa$ (LLM vs.\ human) & 0.578 & 0.335 \\
\quad 95\% bootstrap CI & $[0.471,0.684]$ & $[0.226,0.441]$ \\
Accuracy & 71.9\% & 62.6\% \\
$\kappa_{\text{control}}$ (human--human) & 0.674 & 0.556 \\
Wilcoxon $p$ (LLM vs.\ human) & 0.452 & $<10^{-6}$ \\
Binarized ($\geq 4$) accuracy & 87.1\% & 86.3\% \\
Meets $\kappa \geq 0.70$? & No & No \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Dataset Composition}
\label{subsec:res-data}

Under the composite 1--5 scoring function (Eq.~\ref{eq:score}), the two forms
have very different score profiles. On the \textbf{Raw} form the human ground
truth is skewed toward low-to-mid scores: score~1 (7 reports), score~2 (70),
score~3 (22), score~4 (24), score~5 (16). The LLM's Raw distribution is similar
in shape (6, 75, 14, 23, 21). On the \textbf{Improved} form the human ground
truth shifts strongly upward---score~1 (21), score~2 (1), score~3 (23), score~4
(22), score~5 (72)---consistent with ImproBR raising the executable share from
$28.8\%$ to $67.6\%$. The LLM shifts even further up on the Improved form
(score~1: 13, score~2: 0, score~3: 15, score~4: 3, score~5: 108), assigning
score~5 to 108 of 139 reports. Figures~\ref{fig:raw-distribution}
and~\ref{fig:improved-distribution} show the two distributions.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/raw_score_distribution.png}
\caption{Raw form: reproducibility score distribution, human vs.\ LLM (N=139).}
\label{fig:raw-distribution}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/improved_score_distribution.png}
\caption{Improved form: reproducibility score distribution, human vs.\ LLM (N=139).}
\label{fig:improved-distribution}
\end{figure}

\subsection{RQ1/SQ1: Overall Agreement}
\label{subsec:res-rq1}

On the Raw form, Cohen's kappa between the LLM's composite score and the human
ground truth is $\kappa = 0.578$ (95\% bootstrap CI $[0.471,\,0.684]$), with
accuracy $71.9\%$, macro-F1 $0.666$, macro-precision $0.695$, and macro-recall
$0.655$. On the Landis and Koch scale this is in the upper half of the
\emph{moderate} band, just below \emph{substantial}, and does not reach the
pre-registered $\kappa \geq 0.70$ threshold. On the Improved form, agreement
drops to $\kappa = 0.335$ (CI $[0.226,\,0.441]$), accuracy $62.6\%$---only
\emph{fair} agreement. The two confidence intervals do not overlap, so the
difference between the forms is itself statistically meaningful.

The drop is not merely an artifact of a harder ground truth. As human
reference points we computed $\kappa_{\text{control}}$ directly between Author~1
and Author~2's composite scores on each form: $\kappa_{\text{control}} = 0.674$
on Raw and $\kappa_{\text{control}} = 0.556$ on Improved. The human ceiling does
fall on the Improved form---annotating polished reports, most of which look
complete, is a more subjective judgment---but the LLM falls much further. On
Raw the model sits close below the human ceiling ($0.578$ vs.\ $0.674$, a gap of
$0.096$); on Improved it drops far below it ($0.335$ vs.\ $0.556$, a gap of
$0.221$). Whatever makes the Improved reports harder to judge affects the model
roughly twice as much as it affects a second human annotator.

Figures~\ref{fig:raw-confusion} and~\ref{fig:improved-confusion} show the
confusion matrices. On Raw, disagreement is concentrated near the diagonal and
the model rarely confuses the extremes. On Improved, the mass shifts into the
bottom-right: the model concentrates its predictions on score~5 even where the
human annotator assigned 3 or 4.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/raw_score_confusion_matrix.png}
\caption{Raw form: human vs.\ LLM composite scores (N=139).}
\label{fig:raw-confusion}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/improved_score_confusion_matrix.png}
\caption{Improved form: human vs.\ LLM composite scores (N=139).}
\label{fig:improved-confusion}
\end{figure}

\subsection{SQ2: Agreement by Dimension}
\label{subsec:res-sq2}

Table~\ref{tab:dimension-kappa} and Figure~\ref{fig:comparison-kappa} report
Cohen's kappa computed separately for each reproducibility dimension (S2R on its
binary Executable/Non-Executable judgment; OB and EB rescored on their own 1--3
scales), for both forms.

\begin{table}[!t]
\centering
\caption{Cohen's Kappa by dimension, Raw vs.\ Improved (N=139 each).}
\label{tab:dimension-kappa}
\begin{tabular}{lcc}
\toprule
Dimension & Raw $\kappa$ & Improved $\kappa$ \\
\midrule
Steps to Reproduce (S2R)  & 0.693 & 0.654 \\
Expected Behavior (EB)    & 0.556 & 0.161 \\
Observed Behavior (OB)    & 0.289 & 0.130 \\
Composite (RQ1)           & 0.578 & 0.335 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{figures/comparison_dimension_kappa.png}
\caption{Agreement by dimension, Raw vs.\ Improved, against the pre-registered threshold.}
\label{fig:comparison-kappa}
\end{figure}

The ranking of dimensions is the same on both forms---S2R strongest, then EB,
then OB---but the two forms differ sharply in how much the softer dimensions
degrade. Steps to Reproduce is the most robust: it stays near the human ceiling
on both forms ($\kappa = 0.693$ on Raw, $0.654$ on Improved), because it is
close to a binary, checkable judgment. Expected Behavior and Observed Behavior,
which require judging whether a free-text description is present and sufficient,
hold up moderately on Raw (EB $0.556$, OB $0.289$) but nearly collapse on
Improved (EB $0.161$, OB $0.130$). In other words, the polishing that ImproBR
applies to the behavior descriptions is exactly where the model stops tracking
human judgment: the rewritten text reads as complete, and the model credits it,
even when the human annotator does not.

\subsection{Bias and Sanity Checks}
\label{subsec:res-bias}

The most informative contrast between the forms is in the bias test. On Raw, a
two-sided Wilcoxon signed-rank test on the paired human/LLM composite scores is
non-significant ($W = 337.5$, $p = 0.452$): the model shows no systematic shift,
and its 39 disagreements (28.1\%) split almost evenly into 21 over-scores and
18 under-scores. On Improved, the same test is strongly significant
($W = 158.5$, $p = 7.4\times10^{-7}$, rank-biserial correlation $0.89$): of the
52 disagreements (37.4\%), 47 are cases where the model scores \emph{higher}
than the human and only 5 are lower. The model develops a pronounced optimistic
bias that exists only on the AI-improved reports---it rates 111 of 139 Improved
reports as reproducible (score $\geq 4$) against the humans' 94.

As a coarse sanity check, we binarized both scores at the pre-registered cut
(score $\geq 4 \Rightarrow$ ``Reproducible''). At this granularity the model
performs well on \emph{both} forms: Raw accuracy $87.1\%$ (precision $75.0\%$,
recall $82.5\%$) and Improved accuracy $86.3\%$ (precision $83.8\%$, recall
$98.9\%$). The Improved recall of $98.9\%$ restates the optimistic bias from the
other side: the model almost never misses a report the humans call
reproducible, but only because it calls nearly everything reproducible. The gap
between strong coarse performance and weak fine-grained ordinal agreement---mild
on Raw, severe on Improved---recurs in Section~\ref{sec:discussion}.

\subsection{SQ3: Qualitative Disagreement Analysis}
\label{subsec:res-sq3}

On the \textbf{Raw} form we inspected the 17 reports where the human and LLM
composite scores differ by two or more points, together with the model's
per-dimension rationale text, and found two recurring, opposite-direction
patterns.

\textbf{LLM under-scores reports with implicit trigger conditions.} In several
cases (e.g.,\ MC-300526, MC-300872, MC-301122) the human annotator judged S2R
as \emph{Executable} because the triggering condition could be inferred from the
description, while the LLM required a more explicit, step-by-step trigger and
labeled these reports \emph{Non-Executable}, citing the absence of ``a clear
sequence of actions.'' This matches the \emph{implicit vs.\ explicit}
disagreement pattern anticipated in the study design.

\textbf{LLM over-infers Expected Behavior from contextual cues.} In the opposite
direction (e.g.,\ MC-300143), the human annotator marked Expected Behavior as
\emph{Not Present}, while the LLM inferred an expected behavior from contextual
description and labeled it \emph{Present/Accurate}. On the Raw form these two
patterns roughly cancel, which is why the Wilcoxon test finds no net bias.

On the \textbf{Improved} form the second pattern dominates and the first almost
disappears. Because the ImproBR rewrite fills in explicit steps and fluent
behavior descriptions, the model rarely under-scores for missing structure any
more; instead it takes the polished Observed and Expected Behavior text largely
at face value and credits it as sufficient/accurate, where the human annotator
still judges strictly and marks many as insufficient or inaccurate. This is why
the over-scores outnumber under-scores 47 to 5 and why the OB and EB dimensions
degrade most (Section~\ref{subsec:res-sq2}): the model's willingness to infer
behavior from fluent text, which is a minor and self-cancelling error on Raw,
becomes a systematic bias once every report is written to look complete.
